apiVersion: v1
kind: Service
metadata:
  name: model-orchestrator
  namespace: model-orchestrator
  labels:
    app.kubernetes.io/name: model-orchestrator
spec:
  selector:
    app.kubernetes.io/name: model-orchestrator
  ports:
    - name: http
      port: 8090
      targetPort: http
      protocol: TCP
  type: ClusterIP
