# Copy to ~/.config/model-orchestrator/db.env and chmod 600.
# Consumed by postgres.container (official postgres image init variables).
POSTGRES_DB=orchestrator
POSTGRES_USER=orchestrator
POSTGRES_PASSWORD=change-me
